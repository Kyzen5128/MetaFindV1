# 支線任務

> **四條支線，各自獨立。** 每條標明：**何時可做**、**必須在哪裡匯入主線**、**是否需要 GPU**。
> 主線在 `主線.md`，完整項目清單在 `TASKS.md`。
> 更新於 2026-08-20。

---

## 匯入點總覽

```
S1 讀圖重查   零成本，可立即  ──► 可能改變 M2.3(U-16) 與 S2 的前提
S2 ESSGNN 軸  無重跑          ──► 必須在 M5 Stage 2 之前
S3 n08 節點   需 GPU 22 分    ──► 必須在 M5 Stage 2 之前
S4 Table 2    全新實作        ──► 在 M5 之後
```

**S1 建議最先做** —— 它零成本，而且可能推翻 S2 和 M2.3 的前提。

---

# S1 — 用 39 張論文圖重查所有 UNKNOWN

**對應 `TASKS.md` T7。狀態：可立即進行，零成本。**

## 為什麼存在

`docs/paper/*_source/` 原本**一張圖都沒有**。所有既有稽核都是在「只讀文字」下做的。

本輪解壓後共 39 張：

```
metafind  6      ulip2   15
egnn      8      idesign 10
```

**已經證實有效：**
- `data-preprocess.png` 印著標註 schema → n05 因此白做一輪
- `MetaFind.drawio.png` 標著 `ULIP-2 (Shared)` → 影響 U-16

**剩下 37 張沒逐張看過。**

## 逐項

| # | 動作 |
|---|---|
| S1.1 | 逐張讀 39 張圖，每張記一行：含什麼 PAPER FACT，或「無新規格」 |
| S1.2 | 對照 `docs/graph/` 的 42 項 U 登記，標出：被圖解決／被圖推翻／圖沒提 |
| S1.3 | 對照 `docs/audit/` 的 C1–C8、S1–S6 |
| S1.4 | 補登記：架構圖單一 Fusion Layer vs §2.6 可分別凍結（見主線 M2.3） |
| S1.5 | 補登記：Holodeck 有 `frontView`，MetaFind Figure 2 沒有 |
| S1.6 | 補登記：架構圖標籤錯誤 —— `Text Encoder → I1..IK`、`Image Encoder → T1..TK`，字母對調 |

## 驗收

- [ ] 39 張圖每張一行紀錄
- [ ] U 登記表每項標明是否被圖影響
- [ ] 新矛盾登記成 C 系列

**GPU：** 不需要　**匯入：** 完成後才動 S2 與 M2.3

---

# S2 — ESSGNN 架構軸重構

**對應 `TASKS.md` T1。狀態：等你確認翻案。**

## 為什麼是一個任務

12 處全部是同一個根因的下游：`docs/graph/02_BUILD_STEPS.md:854` 把 U-26 登記成**兩個問題綁在一起**
（「是否共用一條訊息」＋「`f_x` 看到 `h^{l+1}` 還是 `h^l`」）。

程式後來拆成兩個旋鈕，**卻選錯了主軸**。改一處不改其他處會產生自相矛盾的狀態。

## 外部 adjudication 的結論

```
A（two-MLP vs shared-message 是兩個 architecture family）  UNSUPPORTED
B（完整公式是否相同）                                       PROVABLY DIFFERENT
```

**兩者同時成立。B 的承重理由是 `h^{l+1}` 那個上標，與 `m_ij` 符號無關。**

## 兩個 RESEARCH-SIGNIFICANT BUG

```python
# essgnn.py:191-195   有論文依據的軸變成無證據的軸的附屬品
if self.coord_feat is None:
    self.coord_feat = ("current"
                       if self.architecture_family == "appendix_shared_msg"
                       else "updated")

# essgnn.py:491-503   選 appendix_shared_msg 就強制 coord_feat="current"，否則 raise
```

**後果：無法在固定其他條件下單獨測 `coord_feat`** —— 而那是唯一該測的維度。

實測三個變數同時不同（違反 `.claude/rules/experiments.md` §2）：

| family | 參數 | h→gate 線性層數 | coord_feat |
|---|---|---|---|
| `appendix_shared_msg` | 164,737 | 4 | `current` |
| `sec25_two_mlp` | 213,761 | 2 | `updated` |

**只有第三欄有論文依據。深度 4 vs 2 論文完全沒提。**

## 12 處清單

| # | 位置 | 分類 |
|---|---|---|
| S2.1 | `docs/graph/02_BUILD_STEPS.md:854` **根因**，U-26 一號兩義 | STALE ASSUMPTION |
| S2.2 | `essgnn.py:191-195` coord_feat 由 family 推導 | **BUG** |
| S2.3 | `essgnn.py:491-503` family 強制 coord_feat | **BUG** |
| S2.4 | `essgnn.py:90-91` 「different parameter counts」是實作性質非論文 | STALE |
| S2.5 | `essgnn.py:154-165` `<- primary` 措辭 | STALE |
| S2.6 | `resolve_stage2.py:99-131` C1 決策理由 | STALE |
| S2.7 | `docs/audit/E_GRAPH_REVALIDATION.md:173` **最嚴重文件錯誤**，標 `VERIFIED` 而理由是符號差異 | STALE |
| S2.8 | `docs/audit/C_PAPER_CONTRADICTIONS.md:25-77` C1 標題 | STALE |
| S2.9 | `docs/graph/00_FINDINGS.md:1091` | STALE |
| S2.10 | `docs/graph/README.md:17` | DOC |
| S2.11 | `tests/test_resolve_stage2.py:179` 硬斷言鎖進 CI | STALE |
| S2.12 | `tests/test_essgnn.py:24,28` 措辭 | DOC |

## 目標狀態

```
主軸（唯一 paper-conflicted 維度）
    coord_feat: "current" = gate 讀 h^(l)    附錄 Eq.13
                "updated" = gate 讀 h^(l+1)  §2.5 Eq.3

次要 ablation（標 IMPLEMENTATION ABLATION，不是 PAPER ARCHITECTURE FAMILY）
    shared-message vs independent heads

固定：sharing 結構、MLP 深度、hidden 寬度、參數量、初始化、distance、層數
```

> **用詞：`coord_feat` 是 `paper-conflicted`，不是 `paper-backed`。兩個值都是論文寫的，論文自己打架。**

## 驗收

- [ ] 四種 `(family, coord_feat)` 組合都能建構，無強制
- [ ] 固定 `coord_feat` 時兩個 family 參數量與深度相同（可比）
- [ ] 等變測試對四種組合都跑
- [ ] 移除 `test_resolve_stage2.py:179` 硬斷言
- [ ] 全 repo 搜 `two different ESSGNNs` 零命中
- [ ] `tools/check_graph.py` 全過

**GPU：** 不需要　**匯入：** M5 Stage 2 之前　**細節：** `essgnn.md`

---

# S3 — n08 節點文字 + n07 支撐方向

**對應 `TASKS.md` T2 + T3。合併成一條，因為分開跑等於 n08 跑兩次。**

## S3a — 節點文字塌縮

```
assetId 數                  1,467
不重複 node text 數            93     ← object_text() 只吃 category
C(93,2)+93 理論上限          4,371
實際不重複配對                4,242    ← 佔理論上限 97.0%
若以 assetId 去重（前1500房） 114,417
```

`scene_graphs.py:96-104`：

```python
def object_text(category: str) -> str:
    return f"a {humanise(category)}"
```

`Chair_1` … `Chair_50` 全部變成 `"a chair"`。程式自己承認是 ceiling（登記 U-12）。

### 手邊有、卻沒用的真值

| 欄位 | 內容 | 覆蓋 |
|---|---|---|
| `bbox_reported` | **真實公尺尺寸**，與深度殼自量差 5.6 mm | 1,467 / 1,467 |
| `salientMaterials` | 受控詞彙 15 個 | AI2-THOR 可查 |
| `pickupable` / `receptacle` | 真實 affordance | 同上 |
| `parentReceptacles` | 真實擺放對象 | 同上 |

## S3b — 支撐方向被丟掉兩次

```python
scene_graphs.py:155   support.append((parent, idx))              # 方向存在
scene_graphs.py:203   sorted({(min(a,b), max(a,b))})             # 丟掉 ①
semantic_edges_run.py:139   a, b = sorted((ti, tj))              # 丟掉 ②（按字母序）
stage2.py:185-187     rows+=[i,j]; cols+=[j,i]; attrs[-1] 複製    # 反向用同一個 e_ij
```

**現況不是 BUG** —— U-19 已宣告無向為我們的慣例，全程一致。**但是可回收的 INFORMATION LOSS。**

**論文對邊的方向完全沉默**，所以這是 IMPLEMENTATION CHOICE 的另一個值，不是 PAPER-FIDELITY FIX。

## 順帶要改的文件錯誤

| # | 錯誤 | 正確 |
|---|---|---|
| S3.1 | 「列出所有在同一 house 共現的物件 pair」 | 實際是 `support ∪ kNN(k=8)`（`scene_graphs.py:210`） |
| S3.2 | 「25% 的關係是錯的」 | 只能說「25% 含 might/could/may/often 這類詞彙標記，**不能推斷正確性**」 |

## 不要做的

**不要加 no-relation / 過濾。** 論文 §2.3 只說 "obtained by prompting an LLM on object pairs"，
完全沒提過濾。加過濾是發明，且會改 `edge_index` 拓樸（`stage2.py:176` 直接迭代 `sem_edge_ids`）。

## 驗收

- [ ] 不重複 node text 數 > 93
- [ ] 不重複配對數顯著大於 4,242
- [ ] `sem_edge_embeddings.npz` 維度與 `sem_edge_cache.json` 宣告一致
- [ ] 零全零向量、零降級
- [ ] 抽樣人工檢視關係句是否比 category-only 版具體

**GPU：** 需要（n08 實測 22 分鐘）　**匯入：** M5 Stage 2 之前

---

# S4 — Table 2 評估管線

**對應 `TASKS.md` T6。狀態：五個節點完全沒有程式。**

```
n15a / n15b / n15c / n16 / n17   全部只有規格
```

## 本輪讀 I-Design 原始檔取得的依據

| 項目 | 內容 |
|---|---|
| **60 條 prompt** | `idesign_source/tabs/tab_promptlist_{minimal,others}.tex`，四種型態，**每條附房間三維尺寸** |
| **11-20 條「指定佈局」型** | 直接對應 Algorithm 1 —— prompt 內即含初始佈局 |
| **評分維度出處** | MetaFind Table 2 前兩維與 I-Design **逐字相同**，後兩維換掉。MetaFind 有引用 I-Design（`ccelen2024design`），provenance 清楚 |
| **⚠️ 尺度不同** | I-Design **0–10**，MetaFind **1–5**。**兩篇數字不可直接比較** |
| **評分方式** | I-Design：「兩張不同視角**橫向拼接** + 原始 user input，逐場景單獨評分」。MetaFind 只說「scene layouts and rendered views」 |
| **免 LLM 客觀指標** | `NObj`（平均物件數）、`OOB`（任一 bbox 出界即該場景 invalid）、`BBL`（bbox 交集體積平均） |

## 逐項

| # | 項目 | 分類 |
|---|---|---|
| S4.1 | U-27：200 個場景的構造。**現在有 60 條可引用**，差額 140 仍 UNKNOWN | 需決定 |
| S4.2 | n17 用 MetaFind 的四維，**不是** I-Design 的四維 | PAPER-FIDELITY FIX |
| S4.3 | n17 要餵幾張圖／是否拼接／是否附 prompt —— MetaFind 未說 | 需決定 |
| S4.4 | 加入 `NObj` / `OOB` / `BBL`。**不受 D-2、D-4 污染**，是 Table 2 唯一乾淨的量化支撐 | QUALITY |
| S4.5 | 改寫 F18 歸因（見下） | DOC |

## S4.5 — F18 歸因要改

I-Design 的佈局**不是 LLM 出座標**，是：

```
相對場景圖 → 拓樸排序 + cluster 淨空距離 → backtracking 取樣
           → 放不下就回溯 depth d-1 重新取樣
```

而 I-Design 自報 **OOB = 0.0**（完全零出界）。

現有 F18 記錄「Qwen 跑 5 次 0 個場景完成」。
**對照之下更可能是 backtracking 求解不出解，不是 Qwen 失效。**

（`temperature=0.7` / `top_p=1.0` 已正確繼承並記錄於 `tools/idesign_generate.py:15-17`，**非未登記偏離**。）

## 驗收

- [ ] 200 個場景可追溯到 60 條原文，擴充規則明文記錄
- [ ] n17 的 prompt 引用 MetaFind Table 2 四維與 1–5 尺度
- [ ] `NObj` / `OOB` / `BBL` 與 I-Design 定義一致
- [ ] 報告明載兩篇尺度不可直接比較

**GPU：** 需要　**匯入：** M5 之後

---

# 刻意不修（避免下次又去動）

| 項目 | 為什麼不修 |
|---|---|
| n04 單位球正規化 | **必要且正確。** Objaverse 單位跨 1.3e5 倍（47% 超過 10，常見值 1000/100/200），不正規化渲染不出來。絕對尺度從來沒可用地存在過 |
| n08 加 no-relation 過濾 | 論文完全沒提，加了是發明，且會改 `edge_index` 拓樸 |
| `f_x → R³`（§2.5 字面） | 論文寫錯。純量是對的（實測 R³ 等變誤差 0.43 vs 純量 2.2e-16） |
| `onFloor`／`onObject` 準確率 | 天花板來自 n04 正規化，非可修 bug。**寫進限制章節** |
| D-2 / D-5 / D-7 | 已登記的偏離，非缺陷 |
