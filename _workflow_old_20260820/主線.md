# 主線

> **一條路：把模型訓練出來。** 這條線上的每一步都擋著下一步。
> 支線在 `支線任務.md`，完整項目清單在 `TASKS.md`。
> 更新於 2026-08-20。

---

## 主線是什麼

```
n05 標註 ✅ ──► M1 n06 重編碼 ──► M2 Stage 1 前置 ──► M3 Stage 1 訓練
                                                          │
                                                          ▼
                                            M4 gallery 索引 ──► M5 Stage 2
                                                                    │
                                                                    ▼
                                                              M6 評估
```

**判斷標準：** 少了它，下一步就跑不起來 → 主線。
只是讓結果更好、或只影響報告措辭 → 支線。

---

# M0 — n05 標註 ✅ 已完成

```
45,952 筆 v3      隔離 3 筆（0.007%，G3 門檻 2%）
速度 39.0/min     全量 19.3 小時（08-19 11:40 → 08-20 12:50）
```

隔離三筆皆非誤殺：`pom.pom.n.01` synset 格式錯 ×2、`mass=50000kg` 密度檢查 ×1。

備份：`annotations_v1_prompt1/`（45,953）、`annotations_v2_sample/`（200）。**不得刪除。**

---

# M1 — n06 全量重編碼

## 為什麼必須重跑

現有快取 **5,276 筆**，且是用**舊文字模板**編的：

```
舊  ... roughly 0.45 by 0.50 by 0.90 metres, typically placed floor.
新  ... roughly 50 by 45 by 90 centimetres, typically placed on the floor.
```

**舊向量與新標註不一致，混用會讓 Stage 1 在兩種文字上訓練。**

## 動作

```bash
rm -rf data/outputs/embeddings/*
/home/kyzen/miniconda3/envs/MetaFind/bin/python -m metafind.data.encode_text_image
```

## 驗收

- [ ] embeddings 檔數 = 45,952（= n05 v3 筆數）
- [ ] 每個 `.npz` 有 `text` / `image` / `views(11,1280)` 三個鍵
- [ ] over-length text 計數為 0（77 token 上限，`resolve_stage1.py` 有字元上限保護）
- [ ] 抽樣驗證：同類別資產的 text 向量 cosine 顯著高於不同類別

**成本：** 約 4 小時，需 GPU
**阻擋：** M2 的 smoke、M3

---

# M2 — Stage 1 前置（三個阻斷）

`stage1.py:353-359` 開頭就會撞的門檻，缺一不可。

## M2.1 — 跑 `n09_build_splits`（從未執行過）

```
缺 splits.json / eval_protocols.json / stage1_protocol.json
→ stage1.py:356 直接 return 2
```

```bash
/home/kyzen/miniconda3/envs/MetaFind/bin/python -m metafind.data.splits
```

**驗收：** 三個檔案產出、80/20 切分、洩漏 0、`gallery_size` 由切分推導而非寫死。

## M2.2 — 決定 τ ⚠️ 需研究決定

```
論文 3experiments.tex:15   "The temperature is 0.5 for all experiments."   PAPER FACT
現在 stage1_hyperparameters.json   init_temperature=0.07, learnable=True
```

已加 `metafind.models.losses.PAPER_TAU = 0.5`，偏離時建構會 warn。
**但 artifact 還沒改。**

| 選項 | 後果 |
|---|---|
| **A. 改成 0.5 固定** | 忠於論文。**但 0.5 對 InfoNCE 偏大**（CLIP 用 0.07），可能收斂慢、對比訊號弱 |
| B. 維持 0.07 可學 | 工程上穩，但**不能說是在復現論文的數字** |

**建議 A。** 這是論文唯一給出的訓練超參數；連這個都不照，Table 1 就沒有可比性。
若 0.5 訓不起來，**那本身就是值得報告的發現**，不是換掉它的理由。

## M2.3 — 決定 U-16 兩塔是否共享 ⚠️ 需研究決定

**新證據（本輪從論文圖取得）：** `MetaFind.drawio.png` 標著 `ULIP-2 (Shared)`，且**只畫一個 Fusion Layer**。

**但這與 §2.6 有張力：**

> "Only the query-side fusion layer and the ESSGNN module are updated; the gallery encoder is frozen"

若 fuser 只有一個模組，「凍結 gallery」與「訓練 query fuser」不可能同時成立。
`dual_tower.py:315-321` 正是因此在 `fully_shared` 下拒絕 `freeze_gallery()`。

**三個可選值：** `shared_backbone_separate_fusion` / `fully_shared` / `fully_separate`

> ⚠️ `fully_shared` 會讓 Stage 2 無法凍結 gallery。若選它，Stage 2 需要另一套說法。

## 驗收（M2 整體）

- [ ] 三個 json 產出，`tools/check_graph.py` 的 G3 判準通過
- [ ] τ 決定寫入 `stage1_hyperparameters.json` 並記錄分類
- [ ] U-16 決定寫入 `stage1_protocol.json`，附架構圖證據
- [ ] Stage 1 smoke（200 資產、1 epoch）產出完整三段 checkpoint，point encoder 段參數量 > 100 萬

**阻擋：** M3

---

# M3 — Stage 1 訓練

**從未成功跑過一次。`outputs/checkpoints/` 是空的。**

## 已知的實作缺口（不擋跑，但要決定）

`stage1.py:309-338` 的 `build_model()` **不使用 `Stage1RuntimeConfig`**，而是自己從原始 dict 組 config，且只建**一個** backbone。

後果：
- `tower_sharing="fully_separate"`（需要兩個 backbone）**n10 做不到**
- `stage1.py:322-325` 把**同一個 `FusionConfig` 物件**同時給 query 和 gallery
- 34 條 `test_dual_tower.py` 測的是訓練器繞過的那個 class

**要嘛讓 n10 走 `Stage1RuntimeConfig`，要嘛明確記錄為何不走。**

## 驗收

- [ ] smoke 過後才跑全量
- [ ] checkpoint 三段（backbone / tower / loss）都在
- [ ] `assert_checkpoint_covers_optimizer` 通過
- [ ] 訓練曲線寫入 `logs/train_stage1.jsonl`
- [ ] 記錄 git commit、seed、環境、硬體（RTX 5090 32GB）

---

# M4 — gallery 索引

`n11` 暫存 → `G4` 驗證 → `n12` 升級。三個節點同一支程式 `train/gallery_index.py`。

**關鍵：** 編碼器指紋必須隨索引一起存。索引若由漂移過的編碼器產生，loss 與 metrics 都不會報錯，**數字只是自洽地錯**。

## 驗收

- [ ] `gallery_encoder_sha256` 與 `stage1_ckpt.json` 的 sha256 對得上
- [ ] backbone 在 `eval()` 模式（PointBERT `drop_path_rate=0.1`，train 模式會讓索引非決定性）
- [ ] Objaverse 索引與 ProcTHOR 索引分開，永不合併

---

# M5 — Stage 2

**需要支線 S2（ESSGNN 軸）與 S3（n08 節點文字）先落地。**

`train/stage2.py` 迴圈完整但**從未執行**，因此沒有 `IMPLEMENTS-NODE` 標記。

## 前置

- [ ] S2 完成：`coord_feat` 可獨立於 `architecture_family` 設定
- [ ] S3 完成：n08 重跑
- [ ] `stage1_ckpt` 存在
- [ ] `stage2_gallery_index`（n11b）產出

---

# M6 — 評估

Table 1（`n15`）不經過 G6/G7，可在 M4 之後就跑。
Table 2 需要支線 S4。

---

# 現在該做什麼

```
1. M1  清空 embeddings，全量重跑 n06        4 小時，可立即開始
2. M2.1 跑 n09_build_splits                秒級，可與 M1 平行
3. M2.2 / M2.3  τ 與 U-16 的決定            需要你拍板
4. M3  Stage 1 smoke → 全量
```

**M1 和 M2.1 不需要任何決定，可以立刻開始。**
