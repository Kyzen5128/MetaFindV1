# Handoff — 接下來要做什麼

寫於 2026-08-30 02:40。

---

## 現在在跑什麼

**學習率掃描的第 1 次（共 8 次）。02:26 開始。**

```
指令  python -m metafind.train.stage1 --phase dev --epochs 5 --preload \
        --lr 0.00075 --seed 20260830 --repeat-index 1 \
        --out-dir sweep_lr/lr7.50e-4_s20260830

進度  第 3 輪 / 5 輪
速度  560 毫秒／步（沒開 preload 的基準是 589 → 沒變慢）
功率  499W · 68°C · 穩定
成績  epoch 0  七格平均 R@1 0.8582
      epoch 1                0.8432
```

**預計 02:52 左右跑完。**

---

## 整條路，從現在到論文表格

```
  ①  arm 1 跑完                        ← 現在在這裡
  ②  你看過 → 放行剩下 7 次              ← 需要你點頭
  ③  8 次跑完 → 得到「雜訊底線」          ← 這批的真正目的
  ④  決定統計方法                        ← 要等 ③ 的數字才能定
  ⑤  決定最終學習率 + 輪數               ← 需要你點頭
  ⑥  跑正式訓練（--phase final）          ← 需要你點頭
  ⑦  跑 n15 評估（協定 A / B）            ← 需要你點頭，且要 --unseal
  ⑧  產出 Table 1                        ← 第一個能跟論文比的數字
```

---

## 每一步詳細

### ① arm 1（進行中，約剩 12 分鐘）

跑完我會回報四件事：

- 實際花多久（驗證 25 分鐘的估計）
- 有沒有異常（記憶體、溫度、崩機）
- 五輪的分數走勢
- 完整身分紀錄（arm hash / seed / code_revision / code_dirty / 程式碼指紋）

**不會自動往下跑。** 這是你定的「先 1 後 7」。

---

### ② 放行剩下 7 次 —— 需要你一句話

```
時間  約 2.9 小時（7 × 25 分鐘）
順序  已經用固定亂數洗好，寫在 evidence/lr_sweep_plan.md
      洗牌的原因：8/29 崩了九次，萬一又崩不能整批砸在同一個學習率上

2  --lr 0.001   --seed 20260816 --repeat-index 0
3  --lr 0.0005  --seed 20260816 --repeat-index 0
4  --lr 0.00075 --seed 20260816 --repeat-index 0
5  --lr 0.001   --seed 20260830 --repeat-index 1
6  --lr 0.0005  --seed 20260830 --repeat-index 1
7  --lr 0.00025 --seed 20260830 --repeat-index 1
8  --lr 0.00025 --seed 20260816 --repeat-index 0
```

**我會一次跑一個，每個跑完檢查一次。中途有異常就停下等你，不自行重啟。**

---

### ③ 這 8 次會給我們什麼

```
✅ 會給   同一個設定跑兩次差多少（雜訊底線）
          ← 本專案從來沒量過。之前引用的 0.00123 已經撤回，
             因為那兩次的程式碼其實不一樣。
          四個學習率大概落在哪

❌ 不會給 「哪個學習率贏」的裁決
❌ 不會給 「要跑幾輪」的答案
```

**這是你選的「量測，不選拔」。**

---

### ④ 決定統計方法 —— 要等 ③ 的數字

五件事還沒定，而且刻意不現在定：

```
用哪種區間、信心水準多少
最多加到幾顆種子
四個學習率互比要怎麼修正
「跨過門檻就加種子」要怎麼控制誤報
除了三格平均，單一格退步要不要另設保護
```

**理由：δ（1.0 分）要事前定，因為那是「我們要什麼」。**
**統計方法要事後定，因為那是「資料長什麼樣」。**
**現在兩個都定死，等於在另一個地方犯 δ 想避免的錯。**

**⚠ 如果 ③ 量到的雜訊比 1.0 分還大 —— 那這條規則就永遠判不出誰贏。那時候我回來找你，不會自己把 1.0 改小。**

---

### ⑤⑥ 正式訓練 —— 需要你點頭

```
訓練池   36,554 筆（完整的 80%，不再切出 dev_val）
輪數     由 ④ 決定
時間     依輪數而定，25 輪約 2 小時
```

---

### ⑦ n15 評估 —— 需要你點頭，而且要多打一個字

```
協定 C   dev_val 4,569      ← 現在所有分數都是這個。挑模型用的，不能報告
協定 A   test   9,138       ← 從來沒跑過
協定 B   全部   45,692      ← 從來沒跑過

A 跟 B 用的是封存的測試集。程式會拒絕跑，除非加 --unseal。
加了會被記進輸出檔，之後誰都看得到「這次動了測試集」。
```

**為什麼要擋：拿測試集去調參數，等於用考卷練習。之後所有數字都不能算「沒看過的題目」。**

**估時：協定 B 要把 45,692 筆全部過一次模型，還沒量過。這是唯一我沒有時間估計的步驟。**

---

### ⑧ Table 1

**七個條件 × 兩個協定 × R@1/R@5。加上診斷資料：分數差距、並列數、嵌入空間健康度。**

**這是第一個能跟論文對照的東西。**

---

## 同時在跑的另一條線：那個 100 分的謎

**`full` 這一格所有 checkpoint 都是 1.0000，論文是 0.517。**

**四個可能的解釋，目前狀態：**

```
❌ 已排除   低精度四捨五入造成的
            → 高精度重算還是 1.000000000，逐位元相同
            → 這是本專案第一個「用量測」關掉的假說

⏳ 還開著   兩座塔看到一模一樣的輸入
            機制已經從程式碼確認，但機制存在 ≠ 唯一原因
            → 靠 n15 的「打亂答案」對照組測

⏳ 還開著   嵌入空間塌掉了
            → 靠 n15 的空間健康度指標測

⏳ 還開著   這個池子太小、題目太簡單
            → 靠協定 A / B（池子大 2 倍、10 倍）測
```

**三個都要等 n15 真的跑過。**

---

## 你手上還有兩件小事沒回

```
1. .gitignore 要不要開例外
   現在 output/ 整個被 git 忽略，而帳本引用著裡面一個檔
   → 機器一重置，那個證據就沒了，帳本會指著不存在的東西
   MASTER 和我都建議：只放行 output/look/*.json
   （或加上 *.txt，4 個檔共 23 KB。png 和 3D 檔繼續排除，那些有 854 MB）

2. 沒有第二件。上面那一件加上「放行剩下 7 次」就是全部。
```

---

## 規矩（我照著做，寫下來讓你檢查我）

```
GPU 要跑     一定先問你
程式碼要跑   一定先送 Reviewer + Codex 審
要 commit    一定找 MASTER
發現異常     停下來等你，不自行重啟
研究上的選擇 先問 MASTER 討論過沒 → 查上游 → 最後才問你
```

---

## 現在你只要回一件事

**arm 1 跑完之後，看過結果，說「跑剩下的」或「先等等」。**

**其他都不急。**
