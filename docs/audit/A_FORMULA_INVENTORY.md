# A. FORMULA_INVENTORY

Every display formula in the four papers, taken from the authors' arXiv
TeX source. Generated by `tools/build_formula_inventory.py`; do not edit
by hand -- the validation compares this file against the TeX and fails.

## Authority

**arXiv TeX source > published PDF > converted Markdown.** The Markdown
under `docs/paper/*.md` is a convenience copy for prose search and is NOT
authoritative for any formula, dimension, symbol or equation number.

| paper | arXiv | main tex | files read | orphans ignored | display | inline |
|---|---|---|---|---|---|---|
| MetaFind | `2510.05057` | `neurips_2025.tex` | 5 | 0 | 20 | 75 |
| ULIP-2 | `2305.08275` | `main.tex` | 4 | 4 | 3 | 46 |
| EGNN | `2102.09844` | `example_paper.tex` | 10 | 0 | 33 | 523 |
| I-Design | `2404.02838` | `main.tex` | 15 | 0 | 0 | 70 |

Orphans are `.tex` files present in an archive that the main document
never includes. EGNN's archive ships a complete duplicate of itself in a
subdirectory; reading every file on disk would double its equation count.

## Equation numbers

TeX does not store equation numbers -- LaTeX computes them at compile
time. The numbers here are **assigned by this tool** by counting numbered
environments in document order, with each row of a multi-row `align`
taking its own number. They are ours; a disagreement with the published
PDF is a bug in the counter, not in the paper. `equation_label` holds the
authors' own `\label`s and is the stable way to cite.

## What is validated

1. every `exact_latex` is a literal substring of its source `.tex`
2. SHA256 matches `exact_latex`, and survives the JSON round trip
3. no literal ellipsis character inside any formula (`\dots` is fine --
   the check is for a serializer having truncated us)
4. formula ids are unique
5. every formula appears verbatim in this markdown

Formulas live in fenced blocks, never in table cells: a pipe ends a cell
and a width cap inserts an ellipsis.

## MetaFind (20)

### Task Definition

**`MF-1`** — (1) — `2methdology.tex` line 7 — `equation`
- sha256: `2155169dc5cdb805`

```latex
A^* = \arg\max_{A \in \mathcal{A}} \text{sim}(f_{query}(Q), f_{gallery}(A)),
```

### ESSGNN: Scene-Aware Equivariant Graph Encoder

**`MF-U1`** — unnumbered — `2methdology.tex` line 43 — `\[\]`
- sha256: `1202ef68f8db1e54`

```latex
h_i^{(0)} = \text{Concat}(x_i, t_i).
```

**`MF-2`** — (2) — `2methdology.tex` line 50 — `align`
- sha256: `2cb58aebc309cc24`

```latex
h_i^{l+1} &= h_i^l + \sum_{j \in \mathcal{N}(i)} f_h(d_{ij}^l, h_i^l, h_j^l, e_{ij}; \theta_h),
```

**`MF-3`** — (3) — `2methdology.tex` line 50 — `align`
- sha256: `2077c67f528f4e5c`

```latex
x_i^{l+1} &= x_i^l + \sum_{j \in \mathcal{N}(i)} (x_i^l - x_j^l) \cdot f_x(d_{ij}^l, h_i^{l+1}, h_j^{l+1}, e_{ij}; \theta_x),
```

**`MF-U4`** — unnumbered — `2methdology.tex` line 55 — `\[\]`
- sha256: `768a68e8939504f2`

```latex
e_{\text{layout}} = \text{Pooling}(\{h_i^{(L)}\}).
```

**`MF-4`** — (4) — `2methdology.tex` line 62 — `align`
- label: `eq:EGCL_equivariance`
- sha256: `b6a03ca45c5d31d2`

```latex
(R x^{l+1} + T,\, h^{l+1}) = \text{ESSGNN}(R x^l + T,\, h^l,\, E). \label{eq:EGCL_equivariance}
```

### Stage 1: Cross-Modal Alignment Pretraining.

**`MF-5`** — (5) — `2methdology.tex` line 76 — `equation`
- sha256: `ca201498afbbd3e0`

```latex
\mathcal{L}_{\text{pre}} = -\log \frac{\exp(\text{sim}(f_{\text{query}}(Q), f_{\text{gallery}}(A)) / \tau)}{\sum_{A' \in \mathcal{B}} \exp(\text{sim}(f_{\text{query}}(Q), f_{\text{gallery}}(A')) / \tau)},
```

### Stage 2: Layout-Aware Fine-Tuning

**`MF-6`** — (6) — `2methdology.tex` line 84 — `equation`
- sha256: `bbf6ec2a6e0911fb`

```latex
e_{\text{query}} = \text{Fusion}(e_{\text{text}}, e_{\text{img}}, e_{\text{pc}}) + \lambda \cdot e_{\text{layout}},
```

**`MF-7`** — (7) — `2methdology.tex` line 93 — `equation`
- sha256: `78702af688efe262`

```latex
\mathcal{L}_{\text{layout}}^{\text{q2g}} = -\log \frac{\exp(\text{sim}(e_{\text{query}}, e_{\text{gallery}}) / \tau)}{\sum_{e'_{\text{gallery}} \in \mathcal{B}} \exp(\text{sim}(e_{\text{query}}, e'_{\text{gallery}}) / \tau)}, \quad
    \mathcal{L}_{\text{layout}}^{\text{g2q}} = -\log \frac{\exp(\text{sim}(e_{\text{gallery}}, e_{\text{query}}) / \tau)}{\sum_{e'_{\text{query}} \in \mathcal{B}} \exp(\text{sim}(e_{\text{gallery}}, e'_{\text{query}}) / \tau)}
```

**`MF-8`** — (8) — `2methdology.tex` line 100 — `equation`
- sha256: `1b502347ac9fb310`

```latex
\mathcal{L}_{\text{layout}} = \frac{1}{2} \left( \mathcal{L}_{\text{layout}}^{\text{q2g}} + \mathcal{L}_{\text{layout}}^{\text{g2q}} \right).
```

### Equivariance Proof of ESSGNN - Extension to Semantic Embedding

**`MF-9`** — (9) — `appendix.tex` line 25 — `equation`
- sha256: `20bbf147f2110054`

```latex
Q\rm x^{l+1} + g,\; \rm h^{l+1} = \mathrm{ESSGNN}(Q\rm x^l + g,\; \rm h^l,\; E)
```

**`MF-10`** — (10) — `appendix.tex` line 31 — `equation`
- sha256: `806957cd692284a5`

```latex
\rm m_{ij} = \phi_e\left(\rm h_i^l, \rm h_j^l, \| \rm x_i^l - \rm x_j^l \|^2, e_{ij} \right)
```

**`MF-11`** — (11) — `appendix.tex` line 37 — `equation`
- sha256: `a2c435a71d70841a`

```latex
\| Q\rm x_i^l + g - (Q\rm x_j^l + g) \|^2 = \| Q(\rm x_i^l - \rm x_j^l) \|^2 = \| \rm x_i^l - \rm x_j^l \|^2
```

**`MF-12`** — (12) — `appendix.tex` line 43 — `equation`
- sha256: `f81a44e842184020`

```latex
\rm m_{ij}' = \phi_e\left(\rm h_i^l, \rm h_j^l, \| Q\rm x_i^l + g - Q\rm x_j^l - g \|^2, e_{ij} \right) = \rm m_{ij}
```

**`MF-13`** — (13) — `appendix.tex` line 49 — `equation`
- sha256: `4ad56e598cd56bbd`

```latex
\rm x_i^{l+1} = \rm x_i^l + \sum_{j \ne i} (\rm x_i^l - \rm x_j^l) \cdot \phi_x(\rm m_{ij})
```

**`MF-U15`** — unnumbered — `appendix.tex` line 55 — `align*`
- sha256: `f523d1bd8fac5dff`

```latex
Q\rm x_i^l + g + \sum_{j \ne i} \left( Q\rm x_i^l + g - Q\rm x_j^l - g \right) \cdot \phi_x(\rm m_{ij}) &= Q\rm x_i^l + g + Q \sum_{j \ne i} (\rm x_i^l - \rm x_j^l) \cdot \phi_x(\rm m_{ij})
```

**`MF-U16`** — unnumbered — `appendix.tex` line 55 — `align*`
- sha256: `253d6ebb4a426e19`

```latex
&= Q \left( \rm x_i^l + \sum_{j \ne i} (\rm x_i^l - \rm x_j^l) \cdot \phi_x(\rm m_{ij}) \right) + g
```

**`MF-U17`** — unnumbered — `appendix.tex` line 55 — `align*`
- sha256: `bf9f8a94d79aa14f`

```latex
&= Q\rm x_i^{l+1} + g
```

**`MF-14`** — (14) — `appendix.tex` line 64 — `equation`
- sha256: `c41f594547a86afc`

```latex
\rm h_i^{l+1} = \rm h_i^l + \sum_{j \ne i} \phi_h(\rm m_{ij})
```

**`MF-15`** — (15) — `appendix.tex` line 72 — `equation`
- sha256: `20bbf147f2110054`

```latex
Q\rm x^{l+1} + g,\; \rm h^{l+1} = \mathrm{ESSGNN}(Q\rm x^l + g,\; \rm h^l,\; E)
```

## ULIP-2 (3)

### Tri-modal Pre-training

**`U2-1`** — (1) — `main.tex` line 613 — `equation`
- sha256: `100b10cc7e0c770e`

```latex
\mathcal{L}_\text{P2I}=-\frac{1}{2}\sum_i\log\frac{\exp(\mathbf{f}_i^\text{P} \mathbf{f}_i^\text{I}/\tau)}{\sum_j\exp(\mathbf{f}_i^\text{P} \mathbf{f}_j^\text{I}/\tau)}+\log\frac{\exp(\mathbf{f}_i^\text{P} \mathbf{f}_i^\text{I}/\tau)}{\sum_j\exp(\mathbf{f}_j^\text{P} \mathbf{f}_i^\text{I}/\tau)},
```

**`U2-2`** — (2) — `main.tex` line 619 — `equation`
- sha256: `b1bbcb29c53c74a2`

```latex
\mathcal{L}_\text{P2T}=-\frac{1}{2}\sum_i\log\frac{\exp(\mathbf{f}_i^\text{P} \mathbf{f}_i^\text{T}/\tau)}{\sum_j\exp(\mathbf{f}_i^\text{P} \mathbf{f}_j^\text{T}/\tau)}+\log\frac{\exp(\mathbf{f}_i^\text{P} \mathbf{f}_i^\text{T}/\tau)}{\sum_j\exp(\mathbf{f}_j^\text{P} \mathbf{f}_i^\text{T}/\tau)}.
```

**`U2-3`** — (3) — `main.tex` line 625 — `equation`
- sha256: `f67b69a206e1b3a8`

```latex
\min_{E_\text{P}}\mathcal{L}_\text{P2I}+\mathcal{L}_\text{P2T}.
```

## EGNN (33)

### Equivariance

**`EG-1`** — (1) — `sections/background.tex` line 8 — `equation`
- label: `eq:equivariance`
- sha256: `d213f1922e978d3a`

```latex
\label{eq:equivariance}
    \phi(T_g(\rmx)) = S_g(\phi(\rmx))
```

### Graph Neural Networks

**`EG-2`** — (2) — `sections/background.tex` line 25 — `equation`
- label: `eq:gnn`
- sha256: `cda387d2aeb83c8d`

```latex
\label{eq:gnn}
\begin{aligned}
\rmm_{ij} &= \phi_e(\rmh_i^l, \rmh_j^l, a_{ij}) \\
    \rmm_i &= \sum_{j \in \mathcal{N}(i)} \rmm_{i j} \\
    \rmh_i^{l+1} &= \phi_h(\rmh_i^l, \rmm_i) \\
\end{aligned}
```

### Equivariant Graph Neural Networks

**`EG-3`** — (3) — `sections/model.tex` line 13 — `align`
- label: `eq:method_edge`
- sha256: `9ce85693f0f9d8d7`

```latex
\label{eq:method_edge}
\rmm_{ij} &=\phi_{e}\left(\rmh_{i}^{l}, \rmh_{j}^{l},\left\|\rmx_{i}^{l}-\rmx_{j}^{l}\right\|^{2}, a_{i j}\right)
```

**`EG-4`** — (4) — `sections/model.tex` line 13 — `align`
- label: `eq:method_coords`
- sha256: `61a88688911c9355`

```latex
\label{eq:method_coords}
\rmx_{i}^{l+1} &=\rmx_{i}^{l}+ C\sum_{j \neq i}\left(\rmx_{i}^{l}-\rmx_{j}^{l}\right) \phi_{x}\left(\rmm_{ij}\right)
```

**`EG-5`** — (5) — `sections/model.tex` line 13 — `align`
- label: `eq:method_agg`
- sha256: `a6e93bc4fc7ad820`

```latex
\label{eq:method_agg}
\rmm_{i} &=\sum_{j \neq i} \rmm_{ij}
```

**`EG-6`** — (6) — `sections/model.tex` line 13 — `align`
- label: `eq:method_node`
- sha256: `5a37113cb5148e37`

```latex
\label{eq:method_node}
\rmh_{i}^{l+1} &=\phi_{h}\left(\rmh_{i}^l, \rmm_{i}\right)
```

### Analysis on E(n) equivariance

**`EG-U6`** — unnumbered — `sections/model.tex` line 56 — `equation*`
- sha256: `69177bcf948fa2a3`

```latex
Q\rmx^{l+1} + g, \rmh^{l+1}  = \mathrm{EGCL}(Q\rmx^l + g, \rmh^l)
```

### Extending EGNNs for vector type representations

**`EG-7`** — (7) — `sections/model.tex` line 64 — `equation`
- label: `eq:egnn_velocity`
- sha256: `f4047db4d4aec0a7`

```latex
\label{eq:egnn_velocity}
    \begin{aligned}
     
    \mathbf{v}_{i}^{l+1}&= \phi_{v}\left(\rmh_{i}^l\right)\rmv_{i}^\text{init} + C\sum_{j \neq i}\left(\mathbf{x}_{i}^{l}-\mathbf{x}_{j}^{l}\right) \phi_{x}\left(\mathbf{m}_{ij}\right) \\
 \rmx_{i}^{l+1} &=\mathbf{x}_{i}^{l}+ \mathbf{v}_i^{l+1}
    \end{aligned}
```

### Inferring the edges

**`EG-8`** — (8) — `sections/model.tex` line 128 — `equation`
- label: `eq:attention`
- sha256: `29bfa2d93c77229b`

```latex
\label{eq:attention}
   \rmm_i = 
   \sum_{j \in \mathcal{N}(i)}\rmm_{ij} = \sum_{j \neq i} e_{ij}\rmm_{ij}
```

### Graph Autoencoder

**`EG-9`** — (9) — `sections/experiments.tex` line 93 — `equation`
- label: `eq:graph_decoder`
- sha256: `dc28be1d31d4533a`

```latex
\label{eq:graph_decoder}
    \hat{A}_{ij} = g_e(\rmz_i, \rmz_j)=\frac{1}{1 + \mathrm{exp}(
w\left\|\rmz_{i}- \rmz_{j}\right\|^{2} + b
)}
```

### Equivariance Proof

**`EG-U10`** — unnumbered — `sections/appendix.tex` line 17 — `align*`
- sha256: `67cd99f5c44987c3`

```latex
Q\rmx_{i}^{l} + g +C\sum_{j \neq i}\left(Q\rmx_{i}^{l} + g - Q\rmx_{j}^{l} - g\right) \phi_{x}\left(\rmm_{i, j}\right)
    &= Q\rmx_{i}^{l} + g +QC\sum_{j \neq i}\left(\rmx_{i}^{l} - \rmx_{j}^{l}\right) \phi_{x}\left(\rmm_{i, j}\right)
```

**`EG-U11`** — unnumbered — `sections/appendix.tex` line 17 — `align*`
- sha256: `53bcd790f26c91c8`

```latex
&= Q\left(\rmx_{i}^{l} +C\sum_{j \neq i}\left(\rmx_{i}^{l} - \rmx_{j}^{l}\right) \phi_{x}\left(\rmm_{i, j}\right) \right) + g
```

**`EG-U12`** — unnumbered — `sections/appendix.tex` line 17 — `align*`
- sha256: `e3a094bf49a1344e`

```latex
&= Q\rmx_{i}^{l+1} + g
```

### Re-formulation for velocity type inputs

**`EG-U13`** — unnumbered — `sections/appendix.tex` line 31 — `align*`
- sha256: `ebbc49d73b80dcab`

```latex
\rmm_{i j} &=\phi_{e}\left(\rmh_{i}^{l}, \rmh_{j}^{l},\left\|\rmx_{i}^{l}-\rmx_{j}^{l}\right\|^{2}, a_{i j}\right)
```

**`EG-U14`** — unnumbered — `sections/appendix.tex` line 31 — `align*`
- sha256: `9e50c4a2218627f0`

```latex
\rmv_{i}^{l+1}&= \phi_{v}\left(\rmh_{i}^l\right)\rmv_{i}^{\text{init}} +C\sum_{j \neq i}\left(\rmx_{i}^{l}-\rmx_{j}^{l}\right) \phi_{x}\left(\rmm_{i j}\right)
```

**`EG-U15`** — unnumbered — `sections/appendix.tex` line 31 — `align*`
- sha256: `2237559dcdded3a3`

```latex
\rmx_{i}^{l+1} &=\mathbf{x}_{i}^{l}+ \mathbf{v}_i^{l+1}
```

**`EG-U16`** — unnumbered — `sections/appendix.tex` line 31 — `align*`
- sha256: `8e89c73228a9d314`

```latex
\rmm_{i} &=\sum_{j \neq i} \rmm_{i j}
```

**`EG-U17`** — unnumbered — `sections/appendix.tex` line 31 — `align*`
- sha256: `b65b462c3b63c4a8`

```latex
\rmh_{i}^{l+1} &=\phi_{h}\left(\rmh_{i}^l, \rmm_{i}\right)
```

### Equivariance proof for velocity type inputs

**`EG-U18`** — unnumbered — `sections/appendix.tex` line 44 — `align*`
- sha256: `93563c04426b9514`

```latex
\rmv_{i}^{l+1}&= \phi_{v}\left(\rmh_{i}^l\right)\rmv_{i}^{\text{init}} +C\sum_{j \neq i}\left(\rmx_{i}^{l}-\rmx_{j}^{l}\right) \phi_{x}\left(\rmm_{i j}\right)
```

**`EG-U19`** — unnumbered — `sections/appendix.tex` line 44 — `align*`
- sha256: `2237559dcdded3a3`

```latex
\rmx_{i}^{l+1} &=\mathbf{x}_{i}^{l}+ \mathbf{v}_i^{l+1}
```

**`EG-10`** — (10) — `sections/appendix.tex` line 51 — `align`
- sha256: `9150ef76be446fe4`

```latex
\phi_{v}\left(\rmh_{i}^l\right)Q\rmv_{i}^{\text{init}} +C\sum_{j \neq i}\left(Q\rmx_{i}^{l} + g - [Q\rmx_{j}^{l} + g]\right) \phi_{x}\left(\rmm_{i j}\right)
    &= Q\phi_{v}\left(\rmh_{i}^l\right)\rmv_{i}^{\text{init}} +QC\sum_{j \neq i}\left(\rmx_{i}^{l} - \rmx_{j}^{l}  \right) \phi_{x}\left(\rmm_{i j}\right)
```

**`EG-11`** — (11) — `sections/appendix.tex` line 51 — `align`
- sha256: `f6b49574c5ca2163`

```latex
&= Q\left(\phi_{v}\left(\rmh_{i}^l\right)\rmv_{i}^{\text{init}} +C\sum_{j \neq i}\left(\rmx_{i}^{l} - \rmx_{j}^{l}  \right) \phi_{x}\left(\rmm_{i j}\right)\right)
```

**`EG-12`** — (12) — `sections/appendix.tex` line 51 — `align`
- sha256: `464833996ff81ef1`

```latex
&= Q\rmv_{i}^{l+1}
```

**`EG-U23`** — unnumbered — `sections/appendix.tex` line 60 — `align*`
- sha256: `225772bcb32229ff`

```latex
Q\mathbf{x}_{i}^{l}+ g + Q\mathbf{v}_i^{l+1} &= Q\left (\mathbf{x}_{i}^{l} +\mathbf{v}_i^{l+1} \right ) +g
```

**`EG-U24`** — unnumbered — `sections/appendix.tex` line 60 — `align*`
- sha256: `e3a094bf49a1344e`

```latex
&= Q\rmx_{i}^{l+1} + g
```

### Sometimes invariant features are all you need.

**`EG-U25`** — unnumbered — `sections/appendix.tex` line 177 — `align*`
- sha256: `9af0ecf258338b3f`

```latex
\ell_2(Q\rmx_i + t, Q\rmx_j + t) &= \sqrt{} (Q\rmx_i + t - [Q\rmx_j + t])^T(Q\rmx_i + t - [Q\rmx_j + t]) = \sqrt{} (Q\rmx_i - Q\rmx_j)^T(Q\rmx_i - Q\rmx_j)
```

**`EG-U26`** — unnumbered — `sections/appendix.tex` line 177 — `align*`
- sha256: `62d329fa474e44f0`

```latex
&= \sqrt{} (\rmx_i - \rmx_j)^T Q^T Q (\rmx_i - \rmx_j) = \sqrt{} (\rmx_i - \rmx_j)^T (\rmx_i - \rmx_j) = \ell_2(\rmx_i, \rmx_j)
```

**`EG-U27`** — unnumbered — `sections/appendix.tex` line 188 — `equation*`
- sha256: `5ffe1f6d3ed46dfc`

```latex
\ell_2(\tilde{\rmx}_i, \tilde{\rmx}_j) = \ell_2(\rmx_i, \rmx_j) = \ell_2(\rmy_i, \rmy_j) = \ell_2(\tilde{\rmy}_i, \tilde{\rmy}_j).
```

**`EG-U28`** — unnumbered — `sections/appendix.tex` line 193 — `equation*`
- sha256: `ef5aed6f2aa25b3b`

```latex
\rmx_i^T\rmx_i - 2 \rmx_i^T\rmx_j + \rmx_j^T\rmx_j = ||\rmx_i - \rmx_j||_2^2 = ||\rmy_i - \rmy_j||_2^2 = \rmy_i^T\rmy_i - 2 \rmy_i^T\rmy_j + \rmy_j^T\rmy_j
```

**`EG-U29`** — unnumbered — `sections/appendix.tex` line 200 — `equation*`
- sha256: `10b005ebd1f8399e`

```latex
|| \sum_i c_i \rmx_i ||_2 = || \sum_i c_i \rmy_i ||_2 \quad (*).\vspace{-.3cm}
```

**`EG-U30`** — unnumbered — `sections/appendix.tex` line 206 — `align*`
- sha256: `f7500e7f273537f7`

```latex
||\rmy_i - \sum_j c_j \rmy_{i_j}||_2^2 &= \langle \rmy_i, \rmy_i \rangle - 2 \langle \rmy_i, \sum_j c_i \rmy_{i_j} \rangle + \langle \sum_j c_i \rmy_{i_j}, \sum_j c_i \rmy_{i_j} \rangle
```

**`EG-U31`** — unnumbered — `sections/appendix.tex` line 206 — `align*`
- sha256: `2ab3a0e63838d1a2`

```latex
&\stackrel{(*)}{=} \langle \rmx_i, \rmx_i \rangle - 2 \langle \rmx_i, \sum_j c_i \rmx_{i_j} \rangle + \langle \sum_j c_i \rmx_{i_j}, \sum_j c_i \rmx_{i_j} \rangle = \langle \rmx_i, \rmx_i \rangle - 2 \langle \rmx_i, \rmx_i \rangle + \langle \rmx_i, \rmx_i \rangle = 0.
```

**`EG-U32`** — unnumbered — `sections/appendix.tex` line 211 — `equation*`
- sha256: `e3746f88d8fdeb28`

```latex
\langle A \rmx_{i_j}, A \rmx_{i_j} \rangle = \langle \rmy_{i_j}, \rmy_{i_j} \rangle = \langle \rmx_{i_j}, \rmx_{i_j} \rangle
```

## I-Design (0)
