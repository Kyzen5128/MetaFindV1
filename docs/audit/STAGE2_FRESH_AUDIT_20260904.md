# Stage 2 從零對照審計（2026-09-04）

Kyzen（2026-09-03 深夜）：「若 stage 1 確定沒問題了就去實作 stage 2 以及把問題都解決……一切以論文為主，公式呈現必須一致，盡可能做到數據接近。」

做法同 Stage 1 審計：只讀 A0（`docs/paper/metafind_source/metafind_arxiv_v1.html` §2.5、§2.6 Stage 2、§2.7、§3.2、Appendix C），對每個細節寫「論文原文 → 只憑原文的設計 → 我們的程式（檔案:行）→ 判定」。標籤：PAPER FACT / PAPER FIGURE FACT / PAPER-CONSTRAINED INFERENCE / IMPLEMENTATION CHOICE / UNRESOLVED；特殊：DIRECT DEVIATION / PAPER-INTERNAL CONTRADICTION / INVALID EXPERIMENT。

程式碼引用：HEAD `d53f204` 之後。Stage 2 **從未正式訓練過**：只有 `stage2_smoke.log` 的七項檢查（gallery 凍、backbone 凍、query fusion 動、ESSGNN 有梯度、λ 有梯度、場景 dropout 0.2972/20,000 批、雙向損失 τ 0.5）全過。沒有任何 Stage 2 checkpoint。

---

## A. 公式（逐字）與程式對應

**Eq. 6（§2.6）**
```
e_query = Fusion(e_text, e_img, e_pc) + λ · e_layout
```
"where λ is a learnable scalar controlling the contribution of layout information. This residual design allows layout reasoning to enhance retrieval without disrupting the original embedding space."
- 程式 `dual_tower.py QueryTower.forward`：`fused = self.fusion(embeds, present)`；`layout is None → return fused`；否則 `fused + self.lam * layout`，`lam = nn.Parameter(init_lambda)`。✅ 逐項一致。
- `layout=None` 分支就是 Table 1 的 layout-free 評估（§3.2 "evaluating on Objaverse-LVIS (which lacks layout and disables ESSGNN)"）。✅

**Eq. 7 / 8（§2.6）**
```
L_q2g = −log exp(sim(e_q, e_g)/τ) / Σ_{e'_g∈B} exp(sim(e_q, e'_g)/τ)
L_g2q = −log exp(sim(e_g, e_q)/τ) / Σ_{e'_q∈B} exp(sim(e_g, e'_q)/τ)
L_layout = ½(L_q2g + L_g2q)
```
- 程式 `losses.py`：`bidirectional=True` → `½(CE(logits) + CE(logitsᵀ))`；`stage2.py` 建 loss 時 `bidirectional=True`（行 998）。✅
- τ：smoke 記錄 `tau 0.5`（§3.1 "0.5 for all experiments"）。✅
- B = 同批；`unique_positive_batches` 保證一批內 asset_id 不重複（否則 arange(B) 標籤會把真正例算成負例）。✅ 論文沒講，但這是 Eq. 7/8 成立的必要條件。

**Eq. 2（§2.5，特徵更新）**
```
h_i^{l+1} = h_i^l + Σ_{j∈N(i)} f_h(d_ij^l, h_i^l, h_j^l, e_ij; θ_h)
```
- 程式 `essgnn.py ESSGCL.forward`：`m_h = f_h(cat[h_row, h_col, radial, edge_attr])`，`h_next = h + segment_sum(m_h, row)`。✅ 形式一致；f_h 是 MLP（論文 "approximate using multilayer perceptrons"）。
- **d_ij**：正文 Eq. 2 下面寫 `d_ij = ‖x_i − x_j‖_2`（歐氏距離）；**Appendix C Eq. 10–12 用 `‖x_i − x_j‖²`（平方）**。程式 `distance: "squared"`（`essgnn_arch_protocol.json`）→ 採附錄。🟠 **PAPER-INTERNAL CONTRADICTION**（正文 vs 附錄），已知（D0-009）。

**Eq. 3（§2.5，座標更新）**
```
x_i^{l+1} = x_i^l + Σ_{j∈N(i)} (x_i^l − x_j^l) · f_x(d_ij^l, h_i^{l+1}, h_j^{l+1}, e_ij; θ_x)
```
"f_x : R^(2d+1+e) → R^3"
- 程式：`w = f_x(cat[h_for_x_row, h_for_x_col, radial, edge_attr])`，`trans = coord_diff * w`（w 是**標量**），`x + segment_sum(trans)`。
- 兩個不一致：
  1. **f_x 的輸入 h 是第 l 層還是 l+1 層**：正文寫 `h^{l+1}`；附錄 Eq. 13 寫 `φ_x(m_ij)`，而 m_ij 由 `h^l` 算（Eq. 10）。程式 `coord_feat: "current"`（= h^l，附錄讀法）；`"updated"` 選項存在（正文讀法）。🟠 PAPER-INTERNAL CONTRADICTION。
  2. **f_x 的輸出維度**：正文寫 `→ R^3`；附錄 Eq. 13 是標量 `φ_x(m_ij)` 乘向量差。若 f_x 真輸出 R^3 並逐元素乘，**旋轉等變性會壞**（Appendix C 的證明只對標量成立）。程式用標量（附錄）。🟠 PAPER-INTERNAL CONTRADICTION；程式選了讓 Eq. 4 成立的那個。

**h^(0)（§2.5）**
```
h_i^(0) = Concat(x_i, t_i)
```
- **Appendix C 開頭**："We begin by assuming that h^0 is invariant to SE(3) transformations on x" —— 把 x_i 串進 h^(0) 就**不**再對 x 不變，附錄的前提與正文的定義互斥。
- 程式 `h0_mode`：預設 `"semantic"`（h^(0) = t_i，附錄前提）；`"concat_xt"` 選項存在（正文）。`essgnn_arch_protocol.json` **沒有記錄 h0_mode**，走的是程式預設。🔴 **協定漏記**：跑哪一個沒被鎖進 artifact，必須補上。🟠 正文 vs 附錄矛盾本身已記為 D0-009 / RA-1。

**Eq. 4（§2.5，等變性）**
```
(R x^{l+1} + T, h^{l+1}) = ESSGNN(R x^l + T, h^l, E)
```
- 只有「標量 f_x + h^(0) 不含 x + d_ij 只依賴距離」三件同時成立才成立。程式的預設三件都成立；`tests/test_essgnn*.py` 有等變性測試（待確認名稱）。✅ 對附錄版本。

**Pooling（§2.5）**
```
e_layout = Pooling({h_i^(L)})
```
- 論文沒有命名。程式 `pooling: "normalised_sum"`（Kyzen 裁定 DL-077 第 8 項）；另有 mean / sum / max。⚪ IMPLEMENTATION CHOICE（Kyzen 核可）。

---

## B. 圖與資料

| # | 論文原文 | 只憑原文的設計 | 我們 | 判定 |
|---|---|---|---|---|
| B1 | §2.5 "each node v_i represents an object with 3D position x_i ∈ R³ and a text-derived feature t_i ∈ R^d" | 節點 = 房間裡一個物件；t_i 來自文字 | `scene_graphs/*.json`：nodes 含 `asset_id, category, position`；`procthor_node_embeddings.npz` 是每個 asset 的文字向量（凍結 CLIP 文字塔） | ✅；t_i 用哪段文字（類別名？描述？）論文沒說 → ⚪ |
| B2 | §2.5 "Spatial edges are extracted from physical layout constraints (e.g., adjacency, support)" | 物理邊決定鄰接 | `essgnn_edge_protocol.topology = support_union_adjacency`；`physical_relation_encoding = neighbourhood_only`（物理關係只決定拓樸，不進 e_ij） | ✅ 拓樸；物理關係要不要當特徵 → ⚪（Kyzen 整理稿第 39 項） |
| B3 | §2.5 "semantic edges are generated by prompting a large language model (LLM) with object descriptions to produce natural language relation sentences. These sentences are then encoded into dense vectors using a frozen text encoder (e.g., CLIP or BERT)" | LLM 產關係句 → 凍結文字塔 → e_ij | `metafind/data/semantic_edges.py`：LLM（哪一個？見 `procthor_object_text.json._meta.llm_model`）→ CLIP 文字塔；沒快取到的邊用 **learned missing token**（`semantic_missing_representation`） | ✅ 機制；LLM 不是 GPT-4o → DIRECT DEVIATION 的延伸；missing token 是我們的 ⚪ |
| B4 | §2.3 "ProcTHOR … over 10,000 generated houses"；§3.1 "80% … training … 20% … testing" | 10K 房，80/20 | `scene_splits.json`：train 9,600 / test 2,400，seed 20260816（把官方 10k/1k/1k 合起來重切 80/20） | ✅ 對論文 80/20；與官方切法不同（UNRESOLVED，見 Stage 1 計畫） |
| B5 | §2.3 "more than 3,000 unique assets" | — | 12,000 間房實際 1,467 種；Stage 2 gallery index 1,439 個有完整模態的 | 🔴 論文數字無上游支持（已記錄）；1,439 = 有 T/I/P 的子集 |
| B6 | 論文**沒說** ProcTHOR 資產的文字／影像／點雲怎麼來 | — | `stage2_protocol`：`modality_source = ai2thor_isolated`（AI2-THOR 單獨渲染）、`pointcloud_source = multiview_depth_shell`（深度圖回投的殼）、`image_protocol = n04_compatible` | ⚪ **重大 IMPLEMENTATION CHOICE**：ProcTHOR 資產不是 Objaverse GLB，點雲是深度殼不是網格取樣。這會影響 Stage 2 的 gallery 品質 |
| B7 | §2.7 Algorithm 1："retrieve and place one object at a time. After each placement, the scene graph is updated" | 推論時是**逐步前綴**（已放的物件） | 訓練 `encode_query`：**leave-one-out**（整間房減掉目標） | ⚪ 訓練用什麼 context 論文沒說；UNRESOLVED（Kyzen 整理稿第 43 項；GPT 建議 iterative-prefix arm） |
| B8 | 論文沒說 Stage 2 訓練的 query 帶哪些模態 | — | 目標資產的**完整 T/I/P**（查表，不重編碼）+ 場景 context；`query_modality_masking = none` | ⚪ UNRESOLVED（整理稿第 33、42 項） |
| B9 | §2.6 "the layout vector e_layout is omitted in 30% of batches" | 每批一次抽樣，30% 整批不給 layout | `drop = rng.random() < 0.30` 每批一次；smoke 0.2972/20,000 | ✅ |
| B10 | §2.6 "Only the query-side fusion layer and the ESSGNN module are updated during this stage; the gallery encoder is frozen" | 訓 query fusion + ESSGNN + λ；凍 gallery、凍 ULIP 三塔 | `freeze_for_stage2`：gallery 凍、backbone `fuser_only` 凍、只留 `query.fusion.*`、`query.layout_encoder.*`、`layout_weight`；smoke 七項確認 | ✅ |
| B11 | λ 初值：論文沒寫 | — | `init_lambda_ratio = 0.1`（λ₀ = 0.1 × median ‖fused‖/‖layout‖，Kyzen 的數字） | ⚪ IMPLEMENTATION CHOICE（Kyzen 核可） |
| B12 | ESSGNN 層數 / 寬度：論文沒寫 | — | `n_layers 7, hidden 128`（EGNN QM9 上游 `main_qm9.py:34`） | ⚪ UPSTREAM 候選，Kyzen 核可 |
| B13 | Stage 2 的 gallery：論文 "e_gallery"，未說範圍 | — | `gallery_scope = procthor`：1,439 個 ProcTHOR 資產的 Stage 1 gallery 向量（`stage2_gallery_index.json`） | ⚪；Table 1 的 w/ ESSGNN 列則是 Objaverse gallery（§3.2） |

---

## C. Table 1「w/ ESSGNN」怎麼評（PAPER FACT，§3.2 逐字）

> "the fusion layer becomes partially adapted to layout-conditioned features, creating a feature-attribution mismatch when evaluating on Objaverse-LVIS (which lacks layout and disables ESSGNN). A practical mitigation is to maintain two fusion heads: a layout-free head (Stage-1) and a scene-aware head (Stage-2), selected at inference by context availability. Using the Stage-1 head reproduces the 'w/o ESSGNN'…"

設計：
```
w/o ESSGNN 列 = Stage 1 checkpoint；query fusion = Stage 1 的
w/  ESSGNN 列 = Stage 2 checkpoint；query fusion = Stage 2 改過的；layout = None；ESSGNN 不跑；
                gallery 兩列相同（Stage 2 凍 gallery）→ 兩列的差就是 query fusion 被 layout 訓練拉走的量
```
程式現況：**`run_retrieval.py` 沒有 Stage 2 路徑**（grep `stage2|use_layout|layout` 空）。🔴 **缺件**，必須補：載 Stage 2 checkpoint 進 `use_layout=True` 的塔、`layout=None`、gallery 沿用 Stage 1 索引。

---

## D. 錯誤與缺件清單（照重要性）

| 序 | 項目 | 級別 | 動作 |
|---|---|---|---|
| 1 | `run_retrieval.py` 無 Stage 2 評估路徑 | ✅ 已補（2026-09-04） | `--stage2-ckpt-record`：拒絕父 checkpoint 不符；Stage 2 的 query 權重疊在 Stage 1 父塔上；layout None；覆蓋檢查（query.fusion / layout_encoder / λ 都要在 state 裡）；`tests/test_eval_stage2_row.py` |
| 2 | `h0_mode` 沒進 `essgnn_arch_protocol.json` | 🟡 降級：**在程式裡釘住**（`essgnn.PRIMARY_INTERPRETATION`：h0_mode semantic、coords_agg sum、無 edge_proj、不正規化座標差），`from_protocol` 每次都套；不在 artifact | Stage 2 checkpoint 紀錄現在多寫 `primary_interpretation`（2026-09-04） |
| 3 | Stage 2 gallery index 的 Stage 1 父 checkpoint 是哪個 | ✅ 查了 | 舊索引（sha 00f591…）不對應任何現存 arm → 從 **P1** 重建（attrs 疊加目錄，`gallery_index stage2 --stage1-ckpt-record P1`）。P1 為 Stage 2 父 checkpoint：MASTER 選定，Kyzen 可改 |
| 4 | 正文 vs 附錄三處矛盾（d_ij 平方、f_x 輸入層、f_x 輸出維度、h^(0)） | 🟠 PAPER-INTERNAL CONTRADICTION | 主線 = 附錄一致版（Eq. 4 才成立）；正文字面版一個 arm |
| 5 | leave-one-out vs iterative-prefix | ⚪ UNRESOLVED | 兩個 arm |
| 6 | Stage 2 query 模態構法（完整 T/I/P vs 文字為主） | ⚪ UNRESOLVED | 兩個 arm |
| 7 | Pooling / λ₀ / 層數 / 寬度 | ⚪ IMPLEMENTATION CHOICE | 主線用 Kyzen 核可值；掃描排後 |
| 8 | ProcTHOR 資產模態來源（AI2-THOR 渲染 + 深度殼點雲） | ⚪ 重大 IMPLEMENTATION CHOICE | 記錄；Kyzen 說資料集先延後 |
| 9 | ProcTHOR 切分 80/20 vs 官方 | ⚪ UNRESOLVED | 現行 80/20 對論文；Kyzen 定 |

## E. 執行順序

```
1  補 run_retrieval Stage 2 路徑 + h0_mode 進協定（無 GPU）
2  選 Stage 1 父 checkpoint → 重建 stage2 gallery index（1,439 資產，快）
3  Stage 2 十輪先導（附錄一致版、leave-one-out、完整 T/I/P、λ₀ 比例 0.1）
4  評 Table 1 w/ ESSGNN（Objaverse，layout 關）+ Stage 2 自己的 ProcTHOR 檢索
5  arm：正文字面版 ESSGNN；iterative-prefix；文字為主 query
```

## F. 第一次真跑找到的兩個實作錯誤（2026-09-04 凌晨）

| 序 | 錯誤 | 證據 | 修法 | commit |
|---|---|---|---|---|
| F1 | **`build_stage2_model` 建的 Fusion 沒帶 Stage 1 的 `prefusion_norm` / `image_tokens`** → P1（訓練時進 Fusion 前 L2）的權重被載進一個吃 raw 向量的 Fusion（pc 範數 139 對 text 37）；訓練與 w/ ESSGNN 評估器都走這條 | 先導 1：損失從第一步就 2.65 且 1,500 步不動（父模型對自己紀錄應接近 0.8）；query fusion 相對漂移中位 0.55、最大 2.1；w/ ESSGNN 協定 C 掉到 1.3/2.0/17.0/4.8/15.0/15.8/19.7（P1 是 34.7/56.9/86.1/87.6/99.0/92.7/99.7） | 鏡射 `stage1.build_model` 的 FusionConfig；測試比對兩塔設定 | `98fb90e` |
| F2 | **`unique_positive_batches` 的尾巴退化**：一批內 asset 不重複的規則，把最常擺的資產（Cellphone_6 ×200、Pencil_6 ×131）留到最後，尾端 374 批只有 1–2 個樣本；B=1 的 InfoNCE 恆為 0，優化器照踩 | 先導 1 log 最後 300 步 "loss 0.0000"；模擬同一批次序列：1,529 個滿批 + 69 個 B=1 + 205 個 B=2 | 少於 `MIN_BATCH = 8` 的批丟掉並記進 checkpoint 紀錄 | `d3ff975` |

**先導 1 全部標為 INVALID EXPERIMENT**（artifact 搬到 `checkpoints/INVALID_stage2_pilot1_rawfusion/` 留證）。先導 2（builder 修好、其餘同）已重跑，之後接 ProcTHOR 端 S1 / S2-off / S2-on 探針與 Table 1 w/ ESSGNN 列。

順帶記錄：先導 1 的 λ₀ = 96.8（= 0.1 × Fusion 輸出範數中位 968；P1 的 fused 向量範數約 700–1,180），λ 在 1,900 步只動到 96.5——λ 幾乎不學，這一點在先導 2 要再看。

### F3. 先導 2（builder 修好、其餘同先導 1）：Stage 2 的兩個結構性問題浮出來

**數字**（ProcTHOR 端，300 間沒看過的房、19,305 個 leave-one-out 查詢、1,439 資產畫廊）：
```
S1（父，無 layout）        R@1 82.4   R@5 98.1
S2-off（Stage 2 頭，無 layout） 24.2   65.2
S2-on（Stage 2 頭 + λ·ESSGNN）  23.5   63.9
λ 93.46 → 93.13；|Fusion| 1851，|λ·e_layout| 93（一個樣本）
```
Table 1 w/ ESSGNN（Stage 2 頭、layout 關）：C 10.1/15.2/49.2/22.2/56.4/50.5/58.9；D 1.6/4.0/23.4/6.8/31.6/25.5/34.6。父 P1 是 C 34.7/56.9/86.1/…、D 11.6/29.7/66.6/…。論文 w/ 比 w/o 只掉 2–12 分，我們掉 30–40 分。

**Step-0 探針**（`tools/probes/stage2_step0.py`，同一構法、更新前）：loss 2.78、批內 top-1 準確率 0.953。所以「loss 2.65 且不動」不是 bug：τ = 0.5 的 cosine InfoNCE 在 B = 64 時 softmax 幾乎均勻（最大 logit 差 ≈ 2 × 0.1），loss 貼在 ln 64 附近是這個溫度的常態，Stage 1 也一樣。梯度弱但在。

**問題 A：ESSGNN 分支沒有貢獻。** S2-on ≈ S2-off；λ 在 1,600 步只動 −0.3；λ·e_layout 只佔 fused 的 5%。原因不在 λ₀：**query 已經帶了目標資產完整的 T/I/P，答案本來就在 query 裡，layout 沒有可以補的資訊**。這給了整理稿第 42 項（Stage 2 query 構法）一個實驗證據：完整 T/I/P 的 leave-one-out 讓 Eq. 6 的殘差項無事可做。Figure 1 畫的 query 是「文字 + 場景圖」——文字為主的 query 才是 layout 能幫上忙的場合。→ **下一個 arm：Stage 2 訓練時 query 只給文字（或 Stage 1 那種隨機遮罩）**。

**問題 B：fine-tuning 把 layout-free 的頭訓壞。** 50 步內 probe 批的 top-1 從 0.953 掉到 0.84（lr 5e-4 與 5e-5 都是），loss 卻在降：目標在拿「整體 logit 形狀」換「top-1 margin」。Stage 2 的優化器是**沒有 warmup、沒有 cosine 的平坦 AdamW**（Stage 1 有 1 輪 warmup + cosine）。論文說 Stage 2 是 fine-tuning，沒給配方 → 先鏡射 Stage 1 的排程（warmup + cosine）並降 lr；這是 IMPLEMENTATION CHOICE，目的是讓 w/ ESSGNN 的掉幅回到論文的量級，會如實標記。

先導 2 的 artifact 保留（`checkpoints/stage2_full.pt`、`variant_ckpts.json["full"]`、`eval_stage2_pilot2_full_over_P1/`）。

### F4. S2-C / S2-D（lr 5e-5、10% warmup、cosine；1 輪 / 1,500 間房）

```
ProcTHOR 端（300 間沒看過的房，1,439 畫廊，R@1 / R@5）
                        S1（父）        S2-off          S2-on           λ
S2-C  query 只給文字    10.3 / 29.0     12.4 / 43.1     12.2 / 42.7     93.46 → 93.43
S2-D  完整 T/I/P        82.4 / 98.1     36.8 / 78.0     32.8 / 76.8     93.46 → 93.43

Table 1 w/ ESSGNN（Stage 2 頭、layout 關），協定 C ／ D，R@1
                     text  image    pc   T+I  T+PC  I+PC  full
論文 w/ ESSGNN       11.3  10.5  63.2  15.9  41.2  42.0  48.2
論文 w/ ÷ w/o        0.82  0.90  0.84  0.92  0.93  0.92  0.93
P1 父（w/o）   C     34.7  56.9  86.1  87.6  99.0  92.7  99.7
S2-C           C     22.5  37.7  74.3  62.8  86.1  80.1  88.2   ÷父 0.65 0.66 0.86 0.72 0.87 0.86 0.88
S2-D           C     24.9  36.1  71.2  58.2  80.9  73.0  80.1   ÷父 0.72 0.63 0.83 0.66 0.82 0.79 0.80
P1 父（w/o）   D     11.6  29.7  66.6  67.5  95.6  77.8  98.1
S2-C           D      6.0  15.2  48.1  32.5  67.0  55.3  69.6
S2-D           D      7.3  14.2  44.7  29.9  58.6  47.3  57.5
```

**讀法**
1. **λ 在四個 run 裡都不動（93.46 → 93.43）、S2-on ≈ S2-off**，文字 query 也一樣。這不是 bug，是任務結構：ProcTHOR 的資產變體在類別內隨機擺放，「房間裡有什麼」決定不了「是哪一張椅子」；exact-instance R@1 沒有 layout 能補的資訊。這與論文一致——Table 1 的 w/ ESSGNN 比 w/o **低**，論文對 ESSGNN 的證據在場景層（GPT-4o 評分、人評），不在 R@1。我們沒有 GPT-4o 評審，那一半做不了。
2. **配方（warmup + cosine、5e-5）把 layout-free 頭的損傷從先導 2 的 −40 分壓到 −10 ～ −25 分**（協定 C）。S2-C 在協定 C 的 pc 格 ÷父 = 0.86，論文 0.84；full 0.88，論文 0.93。方向與量級第一次落在論文附近；但這只是 1 輪 1,500 間房（論文全量 9,600 間、輪數未知），損傷是下界。
3. 文字 query（S2-C）比完整 T/I/P（S2-D）**更接近論文的 w/ 列**（÷父在 pc/T+PC/I+PC/full 都較高），且符合 Figure 1 畫的 query 形式 → S2-C 的構法為 Stage 2 主線候選。

**先導 2 → S2-C 只動了兩個變數（query 構法、配方）**；S2-D 隔離出配方的貢獻（36.8 vs 24.2 on ProcTHOR S2-off）。
